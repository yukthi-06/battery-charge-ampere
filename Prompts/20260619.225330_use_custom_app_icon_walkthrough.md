# User Prompt
use 'app-icon.png' as the app's icon

---

# Walkthrough - Use Custom App Icon

I have updated the application configuration to point to the new custom app icon.

## Changes Made
### Command
Provided user with the command prompt instruction to copy the icon file:
```cmd
copy app-icon.png app\src\main\res\drawable\app_icon.png
```

### Android Manifest
Modified [AndroidManifest.xml](file:///x:/Projects_X/0_Active/8_Android_APK/05_Early_Stage/Battery_Charge_Ampere_APK_GITHUB/app/src/main/AndroidManifest.xml) to set `android:icon` and `android:roundIcon` to `@drawable/app_icon`.

## Verification Results
- App icon will render using the user's custom `app-icon.png` resource once copied into the resources directory.
